<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        This file contains data extraction rules for Android 12+ (API 31+).
        It disables backup and data extraction for privacy and security.
        Health data should never be backed up to cloud services.
    -->
    <data-extraction-rules>
        <cloud-backup disableIfNoEncryptionCapabilities="true">
            <exclude domain="root" />
            <exclude domain="database" />
            <exclude domain="sharedpref" />
            <exclude domain="file" />
        </cloud-backup>
        <device-transfer>
            <exclude domain="root" />
            <exclude domain="database" />
            <exclude domain="sharedpref" />
            <exclude domain="file" />
        </device-transfer>
    </data-extraction-rules>
</resources>
